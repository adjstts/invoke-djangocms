Invoke tasks for Django CMS.
Copy tasks.py and invoke.yaml files to your Django CMS project to use it.

Config (invoke.yaml):
    python:
        path -- path to project's python interpreter

Tasks (tasks.py):
    startplugin -- create django-cms plugin with given name.
